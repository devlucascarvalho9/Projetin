const fs=require('fs'); const path=require('path');
function findRoot(start){let c=path.resolve(start);for(let i=0;i<6;i++){if(fs.existsSync(path.join(c,'package.json'))&&fs.existsSync(path.join(c,'packages')))return c;const p=path.dirname(c);if(p===c)break;c=p;}return null;}
const root=findRoot(__dirname); if(!root){console.error('Raiz do Kaetram nao encontrada.');process.exit(1);} const statePath=path.join(root,'.v10_5a_roadmap_install_state.json'); const backupSuffix='.antes-v10-5a-roadmap'; const p=(r)=>path.join(root,r);
try{if(!fs.existsSync(statePath))throw new Error('Estado de instalacao do V10.5a nao encontrado. Nada foi revertido.');const state=JSON.parse(fs.readFileSync(statePath,'utf8'));const entries=Object.entries(state.files||{});for(const [rel,info] of entries.reverse()){const dst=p(rel),backup=`${dst}${backupSuffix}`;if(info.existedBefore){if(!fs.existsSync(backup))throw new Error(`Backup ausente: ${rel}`);fs.mkdirSync(path.dirname(dst),{recursive:true});fs.copyFileSync(backup,dst);console.log(`Restaurado: ${rel}`);}else if(fs.existsSync(dst)){fs.rmSync(dst,{force:true});console.log(`Removido arquivo novo: ${rel}`);}}for(const rel of ['packages/client/dist','packages/server/dist']){const d=p(rel);if(fs.existsSync(d))fs.rmSync(d,{recursive:true,force:true});}fs.renameSync(statePath,`${statePath}.rollback-${Date.now()}`);console.log('\nRollback V10.5a concluido. V10.3/V10.4 permanecem intactos.');console.log('Rode yarn build e depois yarn start.');}catch(error){console.error('\nERRO NO ROLLBACK V10.5a:');console.error(error&&error.stack?error.stack:error);process.exit(1);}
