const fs=require('fs'),path=require('path');
const root=path.resolve(process.argv[2]||__dirname);
if(!fs.existsSync(path.join(root,'packages'))||!fs.existsSync(path.join(root,'packages','server'))||!fs.existsSync(path.join(root,'packages','client'))){console.error('\nERRO V11.3C: A pasta recebida nao parece ser a raiz do Kaetram: '+root+'\n');process.exit(1);}
const stateRel='patches/state/v11_3.json',statePath=path.join(root,stateRel);if(!fs.existsSync(statePath)){console.error('ERRO: estado V11.3 nao encontrado.');process.exit(1);}const state=JSON.parse(fs.readFileSync(statePath,'utf8')),backupRoot=path.join(root,state.backupRel);let restored=0;for(const rel of state.affected){const dst=path.join(root,rel),src=path.join(backupRoot,rel);if(state.existed[rel]){if(!fs.existsSync(src)){console.error('Backup ausente: '+rel);process.exit(1);}fs.mkdirSync(path.dirname(dst),{recursive:true});fs.copyFileSync(src,dst);}else if(fs.existsSync(dst))fs.rmSync(dst,{force:true,recursive:true});restored++;}fs.rmSync(statePath,{force:true});console.log(`Rollback V11.3 concluido: ${restored} entradas restauradas/removidas.`);