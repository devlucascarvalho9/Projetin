services:
  mongodb:
    image: mongo:7
    container_name: kaetram-mongodb
    restart: unless-stopped
    ports:
      - "127.0.0.1:27017:27017"
    volumes:
      - kaetram_mongodb_data:/data/db

volumes:
  kaetram_mongodb_data:
